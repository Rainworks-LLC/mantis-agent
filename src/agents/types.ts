/** Agent metadata stored in agent.json */
export interface AgentConfig {
  id: string;
  name: string;
  model: string;
  createdAt: string;
  creatureType: string;
  vibe: string;
  emoji: string;
}
